export default function ContactPage() {
  return (
    <main className="mx-auto min-h-[min(100dvh,720px)] max-w-2xl flex-1 px-4 pb-16 pt-24 md:px-6">
      <h1
        className="text-3xl font-bold text-[#E8ECF4]"
        style={{
          fontFamily:
            'var(--font-space-grotesk), Space Grotesk, system-ui, sans-serif',
        }}
      >
        Contact
      </h1>
      <p className="mt-4 text-[#8B95A8]">
        Formulaire et coordonnées — contenu à venir.
      </p>
    </main>
  );
}
